syntax = "proto3";

service Calculadora {
  rpc Soma (Numeros) returns (Resultado);
  rpc Subtrai (Numeros) returns (Resultado);
}

message Numeros {
  int32 a = 1;
  int32 b = 2;
}

message Resultado {
  int32 resultado = 1;
}